# ThreePhase 考核版需求说明

## 1. 项目定位

新版考核程序与当前教学程序分离，定位为独立的自由操作考核平台。

考核版不再使用教学/工程/考核三种流程模式切换，也不再采用原有步骤评分式考核。系统只保留考核所需的底层仿真、测量、故障、修复和最终并网判定能力。

核心目标：

- 学员在不知道故障类型的情况下自由操作。
- 学员通过测量、记录、判断和黑盒修复，决定是否具备并网条件。
- 最终以 Gen2 是否一次性成功并入母排作为考核通过条件。

## 2. 总体流程

新版考核流程为：

```text
进入考核程序
→ 学员点击随机故障
→ 系统隐藏注入一个故障场景
→ 学员自由操作、测量、记录、查线、修复
→ 学员使用普通 Gen2 合闸按钮执行并母
→ 系统根据本次 Gen2 合闸结果判定通过或失败
```

流程特点：

- 不显示固定步骤进度。
- 不要求按第一步、第二步、第三步推进。
- 不显示正常/异常提示。
- 不弹出故障已发现提示。
- 不要求学员最终选择故障场景。
- 不生成成绩单。
- 不计过程分，只看最终 Gen2 并母结果。

## 3. 考核开始

### 3.1 随机故障入口

第一版保留随机故障入口。

学员点击“随机故障”后：

- 系统随机选择一个已有故障场景。
- 故障内容对学员隐藏。
- UI 只显示考核已开始，不显示具体故障编号、名称、原因或提示。

### 3.2 指定故障

第一版可以暂不暴露给学员。

后续如需教师端或管理员端，可单独设计隐藏入口。

## 4. 自由操作台

新版考核界面应做成自由操作台，而不是步骤面板。

### 4.1 保留能力

应保留：

- Gen1 / Gen2 启停控制。
- Gen1 / Gen2 模式、工作位、合分闸控制。
- 母排拓扑图。
- 实时波形与同期相关显示。
- 万用表测量。
- 相序表测量。
- PT 电压测量。
- PT 二次端子压差测量。
- 黑盒检查与修复入口。
- 测量记录区。
- 最终考核结果显示：等待 / 通过 / 未通过。

### 4.2 取消或隐藏内容

应取消或隐藏：

- 教学模式 / 工程模式 / 考核模式选择。
- 原五步式步骤进度。
- “本步完成 / 未完成”状态。
- 正常、异常、故障已发现等教学提示。
- 随机故障最终场景判定弹窗。
- 成绩单。
- 评分明细。
- 管理员跳步、快速完成、快速记录等明显教学或调试入口。

## 5. 测量与记录

### 5.1 测量功能

测量计算逻辑保留。

包括：

- PT1 / PT2 / PT3 线电压测量。
- PT1 / PT3 相序测量。
- Gen1/PT1 与 PT2 的压差测量。
- Gen2/PT3 与 PT2 的压差测量。
- 回路/接线相关测量。

### 5.2 记录显示

测量记录应保留，但只记录数值和必要上下文。

记录内容可包括：

- 测量对象。
- 测量点位。
- 测量值。
- 测量时间或顺序。
- 操作备注，若后续需要。

UI 不应显示：

- 正常 / 异常。
- 合格 / 不合格。
- 红绿判定。
- 故障定位提示。

### 5.3 底层判断

虽然 UI 不显示对错，底层仍需保留判断逻辑。

底层判断用于：

- 故障物理表现。
- 保护动作。
- 事故合闸判定。
- 最终 Gen2 并母是否成功。

## 6. 黑盒检查与修复

考核版允许学员打开黑盒检查并修复。

原因：

- 若不允许修复，部分故障场景下最终并网条件永远无法满足。
- 考核目标是让学员通过自由排查完成修复并成功并网。

黑盒要求：

- 可查看必要接线状态。
- 可执行允许范围内的接线修复。
- 不显示故障答案式提示。
- 不显示“已定位故障”“请修复某处”等教学提示。

## 7. 最终合闸判定

### 7.1 最终动作定义

最终合闸指：

```text
Gen2 工作位合闸，并入由 Gen1 建立的母排。
```

### 7.2 使用普通合闸按钮

不新增“提交最终合闸”按钮。

学员使用普通 Gen2 合闸按钮完成最终并网尝试。

原因：

- 考核不应设计特殊提示。
- 工程现场如何操作，考核中就如何操作。
- 学员自己判断什么时候可以合闸。

### 7.3 只允许一次 Gen2 最终合闸

Gen2 在考核状态下第一次尝试工作位合闸，即视为最终提交。

规则：

- 一旦 Gen2 工作位合闸被尝试，记录 `final_close_attempted = True`。
- 本次合闸成功，则考核通过。
- 本次合闸失败、事故、保护跳闸或未能并入母排，则考核失败。
- 失败后不允许再次尝试通过修复后重新合闸。

### 7.4 通过条件

建议通过条件为：

```text
Gen1 处于工作位且合闸
Gen2 处于工作位且合闸
母排带电
Gen1 与 Gen2 已形成并联运行状态
未触发事故合闸
未触发保护跳闸导致 Gen2 断开
```

代码层面可最终收敛为类似：

```python
gen1.breaker_position == BreakerPosition.WORKING
gen1.breaker_closed is True
gen2.breaker_position == BreakerPosition.WORKING
gen2.breaker_closed is True
physics.bus_live is True
physics.bus_source == "both"
no_pending_accident
no_trip_after_close
```

### 7.5 失败条件

失败只围绕最终 Gen2 并母结果。

包括：

- Gen2 合闸触发事故逻辑。
- Gen2 非同期合闸失败。
- Gen2 合闸后保护跳闸。
- Gen2 未能成功闭合。
- Gen2 未能与 Gen1 形成并母运行。

不作为失败条件：

- 测量次数多。
- 记录顺序不同。
- 黑盒打开次数多。
- 中途发现或修复路径不够简洁。

## 8. 状态设计建议

新版考核可以增加独立状态对象，例如：

```python
exam_active: bool
fault_hidden: bool
final_close_attempted: bool
exam_result: str  # "pending" | "passed" | "failed"
exam_fail_reason: str
hidden_scenario_id: str
measurement_records: list
```

状态含义：

- `exam_active`: 当前是否处于考核中。
- `fault_hidden`: 当前故障是否对学员隐藏。
- `final_close_attempted`: Gen2 最终合闸是否已经尝试。
- `exam_result`: 考核结果。
- `exam_fail_reason`: 失败原因，仅用于最终结果或教师查看。
- `hidden_scenario_id`: 当前随机故障编号，学员侧不显示。
- `measurement_records`: 自由测量记录。

## 9. 与旧考核模式的差异

旧模式：

- 五步流程。
- 步骤完成判断。
- 故障发现事件。
- 随机故障最终选择。
- 成绩单。
- 多维评分。

新版：

- 自由操作台。
- 无步骤强制。
- 无过程评分。
- 无故障场景选择。
- 无成绩单。
- 只以 Gen2 一次性最终并母结果判定。

## 10. 第一版实现目标

第一版建议先完成：

1. 新建独立考核项目。
2. 保留当前物理引擎、故障注入、测量、黑盒修复能力。
3. 新建自由操作台 UI。
4. 随机故障隐藏注入。
5. 测量记录只显示数值，不显示对错。
6. 监听 Gen2 工作位第一次合闸尝试。
7. 根据该次合闸结果给出通过 / 未通过。
8. 移除旧成绩单与随机故障最终判定。

## 11. 待进一步确认的问题

后续仍需确认：

- 考核开始前是否允许选择“正常无故障”场景。
- 教师端是否需要查看隐藏故障编号。
- 考核失败后是否显示具体失败原因，还是只显示未通过。
- 测量记录是否需要导出。
- 是否需要保存每次操作日志。
- 是否需要超时机制。
