# ThreePhase 考核版强制需求约束

本文档定义 ThreePhase 独立考核版的强制产品规则、界面规则和判定规则。后续所有 UI 文案、测量显示、日志记录、故障修复和最终判定逻辑，均必须符合本文档约束。

## 1. 最高总则

考核版必须遵循以下最高总则：

**系统只呈现工程现象，不解释原因、不提示操作步骤、不替考生判断。**

具体约束：

- 系统必须把判断权交给考生。
- 系统不得向考生提示故障位置、故障原因、下一步操作或正确答案。
- 系统不得用“正常、异常、合格、不合格、正确、错误”等文字评价考生的中间操作。
- 系统不得通过颜色、弹窗、日志或状态栏暗示某次测量是否正确。
- 系统不得把底层判断结果直接暴露给考生。
- 系统可以呈现工程仪表会出现的现象，例如电压值、相序、OL、蜂鸣、过载、无接入、保护等。
- 记录日志必须与仪表本体当时显示的工程现象保持一致，不得额外解释原因。

## 2. 项目定位

考核版必须是独立的自由操作考核程序，不得与教学流程混用。

强制约束：

- 必须取消教学模式、工程模式、考核模式三种流程切换。
- 必须取消原五步式步骤推进。
- 必须取消步骤完成状态。
- 必须取消成绩单、过程评分和评分明细。
- 必须保留底层仿真、测量、故障、修复和最终并母判定能力。
- 考核结果只能由 Gen2 最终一次性并入母排是否成功决定。

## 3. 考核流程

考核版必须采用以下流程：

```text
进入考核程序
→ 考生点击随机故障
→ 系统隐藏注入一个随机故障场景
→ 考生自由操作、测量、记录、检查黑盒并修复
→ 考生自行判断具备并母条件
→ 考生使用普通 Gen2 合闸按钮尝试并母
→ 系统根据本次 Gen2 并母结果判定通过或未通过
```

流程约束：

- 考核开始后，故障编号、故障名称、故障位置和故障原因必须对考生隐藏。
- 考生不得被要求按固定步骤执行。
- 考生不得被要求最终选择故障场景。
- 系统不得弹出“已发现故障”“请检查某处”“请修复某处”等提示。
- 系统只能在最终 Gen2 并母动作后给出通过或未通过。

## 4. 自由操作台

自由操作台必须是考生的主要界面。该界面必须模拟工程操作台，而不是教学步骤面板。

必须保留：

- Gen1 / Gen2 启停控制。
- Gen1 / Gen2 模式控制。
- Gen1 / Gen2 手车位置控制。
- Gen1 / Gen2 普通合分闸控制。
- 母排拓扑图。
- 实时波形与同期相关显示。
- 万用表工具。
- 相序仪工具。
- PT 变比调整入口。
- 黑盒检查与允许范围内的修复入口。
- 测量记录区。
- 考核状态显示：未开始、进行中、判定中、通过、未通过。

必须取消：

- 教学步骤区。
- 本步完成或未完成状态。
- 正常、异常、合格、不合格等过程判断。
- 故障答案式提示。
- 随机故障最终选择弹窗。
- 成绩单。
- 评分明细。
- 管理员跳步、快速完成、快速记录等教学或调试入口。

## 5. 仪表显示约束

仪表显示必须遵循工程现象原则。

### 5.1 万用表

万用表必须只显示仪表本体可见的读数或状态。

允许显示：

- 交流电压值。
- 电阻值。
- OL。
- 蜂鸣或无蜂鸣。
- Err。
- 过载或输入保护。
- ---- 或未接入。

禁止显示：

- 请断开中性点。
- 请切到试验位置。
- 请不起机合闸。
- 请闭合断路器。
- 回路未形成。
- 测量条件异常。
- 读数异常，无法判断通断。
- 正常导通、正常断开、异常导通、检测到接线异常等判定性文字。

回路通断测量必须遵循：

- 正常导通时，可以显示低阻值和蜂鸣。
- 正常断开时，必须显示 OL 或无蜂鸣。
- 回路未形成时，必须与正常断开表现一致，显示 OL 或无蜂鸣。
- 中性点未断开导致寄生通路时，只能表现为对应的读数或蜂鸣现象，不得解释为中性点未断开。
- 带电测通断时，只能表现为 Err、过载或输入保护等仪表现象，不得提示停机或断电。
- 测点未接入或非法时，只能显示 ----、未接入或类似仪表状态，不得提示正确测点。

### 5.2 相序仪

相序仪必须只显示相序测量现象。

允许显示：

- ABC。
- ACB。
- 正序。
- 反序。
- 未接入。
- 等待接线。

禁止显示：

- 该相序正常。
- 该相序异常。
- 请检查 PT1 / PT3。
- 请调换某两相。
- 故障已定位。

### 5.3 波形和同期显示

波形和同期显示必须作为工程状态显示存在。

允许显示：

- 电压、频率、相位、相角差、压差、频差等状态。
- 同期条件的工程状态。

禁止显示：

- 下一步操作提示。
- 故障定位提示。
- 中间过程评分。

## 6. 测量记录约束

测量记录必须是仪表读数的记录，不得是系统解释或教学提示。

记录必须遵循：

- 记录内容必须与仪表本体当时显示一致。
- 记录可以包含测量顺序、测量点位、仪表类型和仪表显示值。
- 记录不得额外写出底层原因。
- 记录不得标记正常、异常、正确、错误、合格、不合格。
- 记录不得用红绿颜色暗示对错。
- 记录不得提示考生下一步该怎么做。

示例约束：

```text
允许：#03 万用表 G1-A ↔ G2-A：OL / 无蜂鸣
允许：#04 万用表 G1-B ↔ G2-C：0.2Ω / 蜂鸣
允许：#05 万用表 PT3-A2 ↔ PT3-B2：105.1 V
允许：#06 相序仪 PT3：ACB / 反序

禁止：#03 回路未形成，请切到试验位置并合闸
禁止：#04 异相异常导通，检测到接线异常
禁止：#05 电压异常，请检查 PT 变比
禁止：#06 相序异常，请检查 PT3
```

## 7. 黑盒检查与修复约束

考核版必须允许考生打开黑盒检查并修复，否则部分故障无法恢复到可并母状态。

黑盒必须遵循：

- 必须允许查看必要接线状态。
- 必须允许在既定范围内进行修复。
- 必须只呈现接线状态本身。
- 不得提示当前黑盒是否为故障点。
- 不得提示应该如何修复。
- 不得提示修复后系统是否已经满足并母条件。
- 不得在 PT3 接线箱弹窗中提供 PT 变比修改入口；PT 变比只能在自由操作台中修改。

## 8. PT 变比约束

PT 变比修改入口必须放在自由操作台中。

约束：

- 必须允许考生修改 PT1、PT2、PT3 的一次侧和二次侧额定值。
- PT 变比 UI 不得提示哪个 PT 存在故障。
- PT 变比 UI 不得标记输入是否正确。
- PT 变比 UI 不得在黑盒弹窗中重复出现。
- E04 等变比故障的底层修复判断可以保留，但不得直接向考生显示故障原因。

## 9. 开关柜位置与合闸约束

Gen1 / Gen2 断路器合闸必须遵循工程位置逻辑。

约束：

- 脱开位置不得合闸。
- 试验位置允许不起机合闸，用于回路测试。
- 工作位置必须起机后才允许合闸。
- 考核中不得通过特殊“提交合闸”按钮完成最终判定。
- Gen2 工作位普通合闸按钮必须作为最终并母动作入口。

## 10. 最终并母判定

### 10.1 最终动作定义

最终动作必须定义为：

```text
Gen2 在工作位使用普通合闸按钮并入由 Gen1 建立的母排。
```

### 10.2 一次性判定

Gen2 在考核进行中第一次尝试工作位合闸，必须视为最终提交。

约束：

- 必须记录 `final_close_attempted = True`。
- 合闸成功并形成双机并母运行，考核通过。
- 合闸失败、事故、保护跳闸或未形成双机并母运行，考核未通过。
- 未通过后不得允许通过再次合闸补救。
- 不得提供“紧急强行合闸”“一键合闸”“跳过判定”等特殊入口。

### 10.3 通过条件

通过条件必须收敛为最终工程状态，而不是过程行为。

必须同时满足：

- Gen1 处于工作位。
- Gen1 已合闸。
- Gen2 处于工作位。
- Gen2 已合闸。
- 母排带电。
- Gen1 与 Gen2 已形成并联运行状态。
- 无待处理事故合闸。
- 无保护跳闸导致 Gen2 断开。

### 10.4 未通过条件

未通过只围绕最终 Gen2 并母结果。

包括：

- Gen2 非同期合闸失败。
- Gen2 合闸触发事故逻辑。
- Gen2 合闸后保护跳闸。
- Gen2 未能成功闭合。
- Gen2 未能与 Gen1 形成双机并母运行。
- 仍存在未修复故障导致并母条件不满足。

不得作为未通过条件：

- 测量次数多。
- 测量顺序不同。
- 记录数量多或少。
- 黑盒打开次数多。
- 修复路径不够简洁。
- 中途出现过错误判断但最终修复完成。

## 11. 底层判断与用户显示边界

底层逻辑可以保留必要判断，但用户显示必须受最高总则约束。

底层允许用于：

- 故障物理表现。
- 保护动作。
- 事故合闸逻辑。
- 黑盒修复状态更新。
- PT 变比故障恢复。
- 最终 Gen2 并母通过或未通过。

用户侧不得显示：

- 底层故障编号。
- 底层故障参数。
- 底层修复是否正确。
- 某个测量结果是否说明故障。
- 某个操作是否符合正确流程。

## 12. 状态对象约束

考核状态对象必须至少支持以下语义：

```python
active: bool
hidden_scenario_id: str
final_close_attempted: bool
final_close_wait_frames: int
sustained_pass_frames: int
result: str  # idle | running | pending | passed | failed
fail_reason: str
measurement_records: list
next_record_no: int
```

约束：

- `hidden_scenario_id` 不得显示给考生。
- `fail_reason` 默认不得作为过程提示使用。
- `measurement_records` 必须遵循测量记录约束。
- `result` 只能表达考核整体状态，不得表达过程评分。

## 13. 重置约束

自由操作台重置必须恢复到可重新开始考核的初始状态。

必须重置：

- 当前随机故障。
- 考核状态。
- 最终合闸尝试状态。
- 测量记录。
- Gen1 / Gen2 运行状态。
- Gen1 / Gen2 合闸状态。
- 自动同步状态。
- 万用表启用状态和表笔测点。
- 相序仪启用状态和接入状态。
- 中性点接地状态。
- PT 相序状态。
- 黑盒接线状态。
- PT 变比状态。
- 波形历史和母排参考源。

## 14. 与旧模式的强制差异

旧教学/考核模式中的以下内容不得在新版考核主流程中出现：

- 五步流程。
- 步骤完成判断。
- 故障发现事件弹窗。
- 随机故障最终选择。
- 成绩单。
- 多维评分。
- 过程得分。
- 教学提示。
- 快速完成或跳步入口。

新版考核必须具备：

- 自由操作台。
- 随机隐藏故障。
- 工程化仪表显示。
- 测量日志原样记录仪表现象。
- 黑盒检查和修复。
- Gen2 一次性最终并母判定。

## 15. 第一版实现边界

第一版必须完成：

- 独立考核项目。
- 自由操作台 UI。
- 随机故障隐藏注入。
- 万用表、相序仪、PT 变比、黑盒检查与修复入口。
- 测量记录区。
- 测量记录去提示化。
- Gen2 工作位第一次合闸尝试监听。
- 最终通过或未通过判定。
- 重置恢复初始工具状态。

第一版不得包含：

- 成绩单。
- 过程评分。
- 教学步骤。
- 故障答案提示。
- 特殊最终提交按钮。
- 强行合闸按钮。

## 16. 待确认事项

以下事项未确认前不得随意实现为考生可见功能：

- 教师端是否需要查看隐藏故障编号。
- 考核失败后是否显示详细失败原因。
- 测量记录是否需要导出。
- 是否需要保存完整操作日志。
- 是否需要超时机制。
- 是否需要教师端指定故障入口。
## 17. PT2 二次侧隐藏故障约束

考核版必须支持 PT2 二次侧三线接反类隐藏故障，并遵守最高总则。

必须支持的故障现象：

- PT2 二次侧 B/C 相接反，实际顺序 ACB。
- PT2 二次侧 A/B 相接反，实际顺序 BAC。
- PT2 二次侧 A/C 相接反，实际顺序 CBA。
- PT2 二次侧整体轮换为 BCA，属于正序轮换假正常。
- PT2 二次侧整体轮换为 CAB，属于正序轮换假正常。

强制约束：

- PT2 单体线电压测量不得直接暴露该类故障，必须表现为工程测得的二次侧电压值及一次侧反推值。
- 相序仪只能显示实际相序；E17-E19 可显示反序，E20/E21 只能显示正序轮换，不得额外提示“整体轮换错误”。
- PT1/PT3 与 PT2 的核相压差必须基于实际物理相位计算，允许同名相高压差、异名相低压差。
- 测量日志只能记录仪表显示值，不得写出 PT2 二次侧接反、错位、故障定位或修复建议。
- PT2 接线盒必须允许考生调整二次侧三相顺序；一次侧可以只读。
- 未修复该类故障时，Gen2 最终并母判定必须为未通过。
- 修复为 ABC 后，且其他并母条件满足时，Gen2 最终并母可以通过。
